VideoOutput {
    id: output_%1
    anchors.left: %2
    anchors.top: %3
    width: parent.width/%4
    height: parent.height/%5
    source: videoProducer_%6
    //property string hurl: "xxx"

    MouseArea {
        anchors.fill: parent
        acceptedButtons: Qt.LeftButton | Qt.RightButton

        onClicked: {
            if (mouse.button === Qt.RightButton)
                contextMenu_%8.popup()
        }
        onPressAndHold: {
            if (mouse.source === Qt.MouseEventNotSynthesized)
                contextMenu_%9.popup()
        }

        onDoubleClicked: {
            wnd.hiResUrl = videoProducer_%7.hiResUrl
            wnd.runHiRes();
        }

        Menu {
            id: contextMenu_%10
            MenuItem {
                text: qsTr("Back")
                onClicked: {
                    wnd.showStartScreen();
                }
            }

            MenuItem {
                text: qsTr("Show hi resolution")
                onClicked: {
                    wnd.hiResUrl = videoProducer_%11.hiResUrl
                    wnd.runHiRes();
                }
            }
        }
    }
}
